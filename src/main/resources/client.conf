akka {
  loglevel = "ERROR"
  actor {
    provider = remote
    warn-about-java-serializer-usage = false
  }
  remote {
    artery {
      enabled = on
      canonical.hostname = "127.0.0.1"
      canonical.port = 0
    }
  }
}
